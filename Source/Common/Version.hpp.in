#pragma once

namespace Version
{
    const char* Readable = "${PROJECT_VERSION}";
    const unsigned Major = ${PROJECT_VERSION_MAJOR};
    const unsigned Minor = ${PROJECT_VERSION_MINOR};
    const unsigned Patch = ${PROJECT_VERSION_PATCH};

    const char* ChangeNumber = "${GIT_CHANGE_NUMBER}";
    const char* CommitHash = "${GIT_COMMIT_HASH}";
    const char* CommitDate = "${GIT_COMMIT_DATE}";
    const char* BranchName = "${GIT_BRANCH_NAME}";
}
